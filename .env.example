# Unraid 推荐：/mnt/user/appdata/happy-kitchen/data
APPDATA_PATH=./data

# 保持 127.0.0.1 绑定；通过 Tailscale Serve 访问，不要映射路由器端口。
HOST_PORT=3000

# Unraid 默认 nobody:users；若目录使用其他属主请相应调整。
PUID=99
PGID=100
TZ=Asia/Shanghai
